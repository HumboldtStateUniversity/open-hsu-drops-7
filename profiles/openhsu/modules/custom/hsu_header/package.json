{
  "name": "hsu_header",
  "version": "1.0.0",
  "description": "Add utility links to the top of HSU Drupal sites.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "del": "^2.2.1",
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^3.1.0",
    "gulp-cssnano": "^2.1.2",
    "gulp-sass": "^2.3.2"
  }
}
